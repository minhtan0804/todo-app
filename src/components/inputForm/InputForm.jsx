import { InputForm, InputTitle } from "./inputFormStyle";

const FormInput = (props) => {
    return (
        <InputForm with={"100%"}>
            <InputTitle height={width}></InputTitle>
        </InputForm>
    );
}
 
export default FormInput;